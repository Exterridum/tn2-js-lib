import Encoder from "./encoder";

export default class XmlEncoder implements Encoder{

    encode(data: any): any {
        return null;
    }

    decode(data: any): any {
        return null;
    }
}